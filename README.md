# OS-Module-in-python
This repo contains info about OS module .<br>
The os module is a built-in Python module that allows your Python program to interact with the operating system.
It provides functions for working with files, directories, environment variables, processes, and more.
